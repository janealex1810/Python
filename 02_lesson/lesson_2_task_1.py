lst = ['🍇', '🍑', '🍐', '🍊', '🍌', '🍎']


if lst:
    first_element = lst[0]
    last_element = lst[-1]
    print(f"Первый элемент: {first_element}")
    print(f"Последний элемент: {last_element}")
else:
    print("Список пуст.")
